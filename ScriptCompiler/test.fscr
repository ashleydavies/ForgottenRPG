import "mem";
string word = "Hello World";

struct Float {
    float x;
}

func void Float::double() {
    this->x = this->x * 2f;
}

Float @x = malloc(sizeof Float);
x->x = 1.0f;
x->double();
x->double();
x->double();
x->double();
print x->x;
print word;
