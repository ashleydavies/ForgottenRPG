print 2 > 1;
if 2 <= 2 {
    print "Hello World!";
}

func int sumN(int x) {
    if x == 0 {
        return x;
    } else {
        return x + sumN(x - 1);
    }
}

func void frog(int x) {
    print sumN(x);
}

for int i = 0; i < 5; i = i + 1 {
    print i;
}

for int m = 10; m < 20; m = m + 2 {
    print m;
    frog(m + 12);
}
