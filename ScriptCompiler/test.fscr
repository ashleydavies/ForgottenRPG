struct test {
    int x;
    int y;
    int z;
    string m;
}

func void test(int n) {
    static int test_old;
    static int test;
    
    if test_old == 0 {
        test_old = 1;
        test = 1;
    }
    
    print test;
    int curr_test = test;
    test = test + test_old;
    test_old = curr_test;
}

for int i = 0; i < 10; i = i + 1 {
    test(42);
}
