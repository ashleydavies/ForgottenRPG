namespace ShaRPG.Entity
{
    // Represents an item on the map for ex
    public class ItemEntity : Entity
    {
        // A map item does not usually need to update.
        public override void Update()
        {

        }
    }
}